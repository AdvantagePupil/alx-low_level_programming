#include <stdio.h>

/**
 * main - entry point
 *
 * description: a c program that prints all single digit numbers of base 10 
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int a;

	a =0;

	if (a < 10)
	{
		printf("a++\n");
	}


	return (0);
}
